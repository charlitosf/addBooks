{
  "name": "addbooks",
  "version": "1.0.0",
  "description": "Webpage to add books to the database",
  "scripts": {
    "start": "parcel src/index.html",
    "build": "rm -r dist/ && parcel build index.html",
    "deploy": "npm run build && firebase deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.7.0",
    "parcel": "^2.7.0",
    "process": "^0.11.10"
  },
  "dependencies": {
    "@popperjs/core": "^2.11.6",
    "bootstrap": "^5.2.1",
    "dotenv": "^16.0.2",
    "firebase": "^9.9.4"
  }
}
